   server {
           listen {{ network_topology["private"]["address"] }}:{{ nova.novncproxy_port.cont }};
           include common/ssl.conf;

           location / {
                   proxy_pass http://novncproxy;
                   include common/proxy-headers.conf;
           }
   }
