dsl_version: 0.1.0
service:
  name: nova-novncproxy
  ports:
    - {{ nova.novncproxy_port }}
  containers:
    - name: nova-novncproxy
      image: nova-novncproxy
      daemon:
        command: nova-novncproxy --config-file /etc/nova/nova.conf
        files:
          - nova.conf
        dependencies:
          - nova-db-create-and-sync
files:
  nova.conf:
    path: /etc/nova/nova.conf
    content: nova.conf.j2
    perm: "0600"
